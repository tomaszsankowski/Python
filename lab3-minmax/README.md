# Lab 3: MinMax Algorithm

This project implements the MinMax algorithm with alpha-beta pruning using the Connect 4 game as an example. It includes various agents (random, minmax, alpha-beta) and game modes (PvP, PvA, AvA).
